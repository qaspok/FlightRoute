{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "Flights",
	"description": "Contains a list of flights; origin, destination and cost",
	"type": "object",
	"properties": {
		"startname": {
			"description": "The name of the city in which the flight starts",
			"type": "string",
			"minLength": 0,
			"maxLength": 50
		},
		"startcode": {
			"description": "Airport code for the origin city",
			"type": "string",
			"minLength": 0,
			"maxLength": 3
		},
		"endname": {
			"description": "The name of the city in which the flight ends",
			"type": "string",
			"minLength": 0,
			"maxLength": 50
		},
		"endcode": {
			"description": "Airport code for the desitination city",
			"type": "string",
			"minLength": 0,
			"maxLength": 50
		},
		"cost": {
			"type": "number",
			"minimum": 0,
			"exclusiveMinimum": true
		}
	}
}